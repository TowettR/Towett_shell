#include <stdio.h>

/**
 * main - program that gives the sum of two numbers
 * @a: integer
 * @b: integer
 * Return: Always 0
 */
int main(int a, int b)
{
	int sum;

	sum = (a + b);
	printf("sum is %d\n", sum);
	return (0);
}
